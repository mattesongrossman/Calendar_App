import React from "react"
// import "./style.css";

const Event = props => {
  const {
    event_id,
    event_name,
    event_time,
    event_description,
    event_type
  } = props
  return (
    <div className="Event">
      <h2 />
    </div>
  )
}

export default Event
